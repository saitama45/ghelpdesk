{
  "name": "ghelpdesk-e2e",
  "private": true,
  "//": "The app's root package.json sets \"type\": \"module\", which would make Node parse these CommonJS specs as ESM and fail on require(). This file scopes the module type to the e2e folder. It intentionally declares no dependencies — Playwright comes from the global kit at ~/.claude/qa.",
  "type": "commonjs"
}
